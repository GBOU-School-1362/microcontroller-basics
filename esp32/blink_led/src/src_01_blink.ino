// Пример 1: Простое мигание светодиодом

// Сначала указываем, к какому пину подключён светодиод.
// На большинстве плат ESP32 встроенный светодиод находится на пине 2.
#define LED_PIN 2

void setup() {
  // Функция setup() выполняется один раз при старте.
  // Мы говорим, что пин LED_PIN будет использоваться для вывода (OUTPUT).
  pinMode(LED_PIN, OUTPUT);
}

void loop() {
  // Функция loop() выполняется бесконечно много раз (цикл).
  // Включаем светодиод, подавая высокий уровень на пин.
  digitalWrite(LED_PIN, HIGH);
  delay(500); // Ждём 500 миллисекунд (0.5 секунды)
  // Выключаем светодиод.
  digitalWrite(LED_PIN, LOW);
  delay(500); // Ждём ещё 0.5 секунды

  // Почему так? Это базовая “мигалка” — классический первый проект для освоения микроконтроллеров.
  // Мы учимся задавать пины и видим, как работает бесконечный цикл.
}